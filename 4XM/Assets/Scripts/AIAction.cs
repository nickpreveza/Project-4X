using System.Collections;
using System.Collections.Generic;
using UnityEngine;




/*
 
 public List<ResourceAction> harvestActions = new List<ResourceAction>();
    public List<BuildingAction> buildingActions = new List<BuildingAction>();

    public void EvaluteHexActionsForPlayer(AbilityTree tree)
    {
        foreach(ResourceAction action in harvestActions)
        {
            action.canBeHarvested = FindIfHarvestPossible(tree, action.resource);
        }
    }

    public bool FindIfBuildingPossible(AbilityTree tree, BuildingType building)
    {
        switch (building)
        {
            case BuildingType.FarmMaster:
                if (tree.farmMasterBuilding)
                {
                    return true;
                }
                break;
            case BuildingType.FarmWorked:
                if (tree.farmMasterBuilding)
                {
                    return true;
                }
                break;
        }

        return false;
    }

    public bool FindIfHarvestPossible(AbilityTree tree, ResourceType resource)
    {
        switch (resource)
        {
            case ResourceType.ANIMAL:
                if (tree.animalHarvest)
                {
                    return true;
                }
                break;
            case ResourceType.MINE:
                if (tree.mineHarvest)
                {
                    return true;
                }
                break;
            case ResourceType.FARM:
                if (tree.farmHarvest)
                {
                    return true;
                }
                break;
            case ResourceType.FOREST:
                if (tree.forestHarvest)
                {
                    return true;
                }
                break;
            case ResourceType.FISH:
                if (tree.fishHarvest)
                {
                    return true;
                }
                break;
            case ResourceType.FRUIT:
                if (tree.fruitHarvest)
                {
                    return true;
                }
                break;
        }

        return false;
    }
 */
